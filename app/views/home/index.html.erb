<!DOCTYPE html>
<html>
  <head>
    <title>Bienvenue sur notre site de potins !</title>
  </head>
  <body>
  <div class="title-container"> 
  <h1>Bienvenue sur notre super site de potins !</h1>
  </div> 

  <div class="container">
  <div class="row">
    <% @gossips.each do |gossip| %>
      <div class="col-md-4">
        <div class="card">
          <div class="card-body">
            <h5 class="card-title"><%= gossip.title %></h5>
            <p class="card-text"><%= gossip.content.truncate(100) %></p>
            <a href="<%= gossip_path(gossip) %>" class="btn btn-primary">Voir plus</a>
          </div>
        </div>
      </div>
    <% end %>
  </div>
</div>


<h2> <%= link_to 'S\'inscrire', new_user_path %> </h2>
 